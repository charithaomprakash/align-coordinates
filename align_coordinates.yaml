# align_coordinates.yaml


# install: conda env create -f align_coordinates.yaml
# update:  conda env update -f align_coordinates.yaml
name: align_coordinates
channels:
  - anaconda
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - pytest-shutil
    - numpy
    - spyder
    - matplotlib
    - pathlib
    - pandas
    - seaborn
    - ruamel.yaml

